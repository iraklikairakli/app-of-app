# API Gateway — production overrides
hpa:
  enabled: true
  maxReplicas: 8
  targetCPU: 75

configMap:
  enabled: true
  data:
    LOG_LEVEL: "warn"
    DB_HOST: "postgres.production:5432"

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi
