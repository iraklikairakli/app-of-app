# =============================================================================
# Production Cluster 1 (us-east-1) — Cluster-Wide Defaults
# =============================================================================

replicaCount: 3

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi
