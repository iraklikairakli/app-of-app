# User Service — shared config (all environments)
image:
  repository: registry.example.com/user-service

configMap:
  enabled: true
  data:
    CACHE_HOST: "redis.cache:6379"

# Secrets managed via External Secrets Operator
secret:
  enabled: true
