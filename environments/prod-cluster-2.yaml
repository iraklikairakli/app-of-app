# =============================================================================
# Production Cluster 2 (eu-west-1) — Cluster-Wide Defaults
# =============================================================================

replicaCount: 3

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi
